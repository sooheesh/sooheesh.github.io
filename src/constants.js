const IS_A_MINE = -1;
const IS_FLAGGED = -1;
const IS_VISIBLE = 1;

const BOARD_CLEARED = 0;
const BOARD_SET = 1;
const MINE_DETONATED = 2;
const GAME_CLEARED = 3;

export default {
    IS_A_MINE,
    IS_FLAGGED,
    IS_VISIBLE,
    BOARD_CLEARED,
    BOARD_SET,
    MINE_DETONATED,
    GAME_CLEARED
}

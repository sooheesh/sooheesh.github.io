import image from '../resources/minesweeper.png';

const gStyles = {
    image: {
        backgroundImage: `url(${image})`,
    },
    row: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
}

export default gStyles;
